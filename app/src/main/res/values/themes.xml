<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Individualna" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

    <!-- Toolbar that uses light (white) text/icons on the colored app bar -->
    <style name="ThemeOverlay.Individualna.Toolbar" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <!-- Smaller tab labels so longer Cyrillic titles fit on a single line -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">11sp</item>
        <item name="android:textAllCaps">true</item>
    </style>

</resources>
